<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('partial/head-bootstrap') %>
    <title>Document</title>
</head>
<body>
    <%- include('partial/nav-logged' , { invId }) %>

    <div class="container mx-auto flex flex-col  my-20 py-10">
        <h1 class="text-7xl text-center mb-[.4em]">Beli Saham proyek <%= project.nameProject %>?</h1>

        <p style="color: red;"><%= error ? error: '' %></p>
        <form action="/investor/<%= investor.id %>/projectList/<%= project.id %>/buySaham" method="post" class="mx-auto border border-black py-16 px-10">
        <div class="flex flex-col my-2">
            <label class="text-2xl">Masukan jumlah load</label>
            <input type="number" name="load" class="border border-black text-2xl  p-1" placeholder="<%= formatCurent(project.minimumLoad) %>">
        </div>
        <button type="submit" class="bg-[#64748b] p-2 rounded text-white mx-auto">BUY</button>
        </form>
    </div>
</body>
</html>